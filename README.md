# BinktermPHP AX.25 KISS Bridge

> **This adapter is experimental.** Features may be incomplete and behaviour may change between releases without notice. Please [report issues to GitHub](https://github.com/awehttam/binkterm-php/issues).

A standalone bridge daemon that connects a KISS TNC to a [BinktermPHP](https://lovelybits.org/binktermphp) BBS, giving amateur packet radio stations access to the PacketBBS over AX.25.

```
[Radio Hardware]
     |
     | serial / USB
     v
[KISS TNC]           (hardware TNC, or software TNC such as Direwolf)
     |
     | KISS over TCP / serial
     v
[ax25_kiss_bridge]   (this repo)
     |
     | HTTP API (Bearer token)
     v
[BinktermPHP BBS]
```

Each AX.25 source callsign is treated as a separate PacketBBS node. Operators send plain-text BBS commands as AX.25 UI frames addressed to the BBS callsign. The bridge forwards commands to the BBS and transmits responses back over air.

---

## Requirements

- PHP 8.2 or later with the `curl` extension
- Composer
- A KISS-capable TNC: any hardware TNC in KISS mode, or a software TNC such as [Direwolf](https://github.com/wb2osz/direwolf)
- A running BinktermPHP instance (1.11.0 or later)

---

## Installation

```bash
git clone https://github.com/awehttam/binktermphp-ax25kiss.git
cd binktermphp-ax25kiss
composer install
cp config.example.json config.json
```

Edit `config.json` with your callsign, BBS URL, and API key (see [Configuration](#configuration) below).

---

## BBS Setup

1. Open your BinktermPHP admin panel and go to **Packet BBS → Nodes**.
2. Add a new node:
   - **Node ID**: your bridge callsign (e.g. `N0BBS-1`)
   - **Interface Type**: `AX.25 TNC`
3. Generate an API key for the node and paste it into `config.json` as `api_key`.

---

## Configuration

Copy `config.example.json` to `config.json` and fill in the values:

| Key | Description |
|---|---|
| `mycall` | Your BBS station callsign (e.g. `N0BBS-1`). Used as the AX.25 source when transmitting. |
| `bridge_node_id` | Node ID registered in the BBS admin panel. Usually the same as `mycall`. |
| `api_key` | Bearer token generated by the BBS for this node. |
| `bbs_url` | BBS base URL without trailing slash (e.g. `http://192.168.1.10:1244`). |
| `tnc.type` | `tcp` for software TNCs; `serial` for hardware TNCs. |
| `tnc.host` | TCP hostname for software TNC (default: `localhost`). |
| `tnc.port` | TCP port for software TNC (Direwolf default: `8001`). |
| `tnc.device` | Serial device for hardware TNC (e.g. `/dev/ttyUSB0`). |
| `tnc.baud` | Serial baud rate (default: `1200`). |
| `poll_interval_sec` | How often to poll the outbound queue for active callsigns (default: `15`). |
| `active_ttl_sec` | Seconds of inactivity before a callsign is dropped from the poll list (default: `900`). |
| `max_frame_info` | Maximum AX.25 information field size in bytes. Long responses are split across multiple frames (default: `236`). |
| `log_level` | `DEBUG`, `INFO`, `WARNING`, or `ERROR` (default: `INFO`). |
| `log_file` | Path to log file. Set to `""` to disable file logging. |
| `log_echo` | Set to `true` to also print log output to stdout. |
| `beacon.enabled` | Send periodic beacon frames (default: `true`). |
| `beacon.interval_sec` | Seconds between beacons (default: `1800` = 30 minutes). A beacon is also sent immediately on TNC connect. |
| `beacon.dest` | AX.25 destination callsign for beacon frames (default: `"BEACON"`). |
| `beacon.text` | Text transmitted in each beacon frame. |

---

## Running

### Foreground (development / testing)

```bash
php ax25_kiss_bridge.php --config=config.json --echo-log
```

### Background daemon

```bash
php ax25_kiss_bridge.php --config=config.json --daemon --pid-file=/var/run/ax25_kiss_bridge.pid
```

Stop the daemon:

```bash
kill $(cat /var/run/ax25_kiss_bridge.pid)
```

### systemd service

```ini
[Unit]
Description=BinktermPHP AX.25 KISS Bridge
After=network.target

[Service]
Type=simple
User=bbs
WorkingDirectory=/opt/binktermphp-ax25kiss
ExecStart=/usr/bin/php ax25_kiss_bridge.php --config=config.json --echo-log
Restart=on-failure
RestartSec=10

[Install]
WantedBy=multi-user.target
```

---

## Direwolf (software TNC) quick-start

In your `direwolf.conf`:

```
MYCALL  N0BBS-1
KISSPORT 8001
```

Start Direwolf:

```bash
direwolf -c direwolf.conf
```

The bridge will connect to `localhost:8001` by default.

---

## BBS commands

Operators send commands as plain text in AX.25 UI frames addressed to the BBS callsign. The full command set is documented in the BinktermPHP admin panel under **Packet BBS → Help**.

Common commands:

| Command | Description |
|---|---|
| `LOGIN <user> <code>` | Authenticate (TOTP) |
| `HELP` | List available commands |
| `N` | List netmail |
| `NR <id>` | Read netmail |
| `E <tag>` | Open echo area |
| `EM <id>` | Read echomail |
| `M` | Next page |
| `Q` | Quit / end session |

---

## Licence

BSD 3-Clause — see [LICENSE](LICENSE).

This project is part of the [BinktermPHP](https://lovelybits.org/binktermphp) ecosystem.
